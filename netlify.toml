[build]
  base = "kidzkapers"       # Netlify will cd into this folder first
  command = "npm install && npm run build"
  publish = "kidzkapers/build"  # Where the built files are

[build.environment]
  NODE_VERSION = "18"